{
  "name": "@app/api",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "build": "bun build:for --current",
    "build:for": "tool-cli compile src/main.ts --prefix api --clean --bytecode --minify --sourcemap",
    "clean": "git clean -xdf .turbo dist node_modules",
    "dev": "bun run --watch src/main.ts",
    "ts:check": "tsc --noEmit"
  },
  "dependencies": {
    "@elysiajs/swagger": "^1.3.0",
    "@yolk-oss/elysia-env": "^3.0.0",
    "defu": "^6.1.4",
    "elysia": "^1.3.5"
  },
  "devDependencies": {
    "@tool/cli": "workspace:*",
    "@tool/tsconfig": "workspace:",
    "@types/bun": "catalog:tooling",
    "@types/node": "catalog:tooling",
    "typescript": "catalog:tooling"
  }
}
